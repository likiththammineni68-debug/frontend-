# Environment Configuration for Deployment

# Development
VITE_API_URL=http://localhost:3001/api
VITE_APP_ENV=development

# Production (update these with your deployed backend URL)
# VITE_API_URL=https://your-backend-url.com/api
# VITE_APP_ENV=production

# Local Network Access
# VITE_API_URL=http://192.168.31.176:3001/api
# VITE_APP_ENV=local-network

# Ngrok helper variables
# After you run `ngrok http <port>` set `NGROK_URL` to the host part (no protocol)
# Example: NGROK_URL=abcd-12-34-56.ngrok-free.app
NGROK_URL=
# When exposing the backend through ngrok, set the frontend API base URL accordingly
# Example: VITE_API_URL=https://abcd-12-34-56.ngrok-free.app/api